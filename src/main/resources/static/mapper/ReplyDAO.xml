<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.study.boardflab.mybatis.dao.ReplyDAO">
    <!--    Create    -->
    <insert id="create">
        insert into reply (post_id, content, written_user, non_mem_nick, non_mem_pw)
        values (#{postId}, #{content}, #{writtenUser}, #{nonMemNick}, #{nonMemPw})
    </insert>



    <!--    Read    -->
    <select id="getList" resultType="com.study.boardflab.mybatis.vo.ReplyVO">
        select r.id as id, r.written_user as writtenUser, u.nickname as memberNick, r.content as content, r.non_mem_nick as nonMemNick
        from reply r
            join user u on r.written_user = u.account_id
        where post_id = #{postId}
        order by r.written_time desc
        limit #{limit}
        offset #{offset}

    </select>
    <select id="find" resultType="com.study.boardflab.mybatis.vo.ReplyVO">
        select id, post_id as postId, content, written_user as writtenUser, non_mem_nick as nonMemNick, non_mem_pw as nonMemPw
        from reply
        where id = #{id}
    </select>

    <!--    Update    -->
    <update id="update">
        update reply set content = #{content} where id = #{id}
    </update>

    <!--    Delete    -->
    <delete id="delete">
        delete from post where id = #{id}
    </delete>

</mapper>